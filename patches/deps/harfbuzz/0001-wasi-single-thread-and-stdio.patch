diff --git a/meson.build b/meson.build
index d3caf3c4..23274c78 100644
--- a/meson.build
+++ b/meson.build
@@ -349,7 +349,9 @@ endif
 
 # threads
 thread_dep = null_dep
-if host_machine.system() != 'windows'
+# HB_NO_MT makes locks and atomic reference counts local to one thread.  Do not
+# add WASI's -pthread target features to that build.
+if host_machine.system() not in ['windows', 'wasi']
   thread_dep = dependency('threads', required: false)
 
   if thread_dep.found()
diff --git a/src/hb-blob.cc b/src/hb-blob.cc
index 9eb484a9..e4bd01cf 100644
--- a/src/hb-blob.cc
+++ b/src/hb-blob.cc
@@ -29,12 +29,12 @@
 #include "hb.hh"
 #include "hb-blob.hh"
 
-#ifdef HAVE_SYS_MMAN_H
+#if defined(HAVE_SYS_MMAN_H) && !defined(HB_NO_MMAP)
 #ifdef HAVE_UNISTD_H
 #include <unistd.h>
 #endif /* HAVE_UNISTD_H */
 #include <sys/mman.h>
-#endif /* HAVE_SYS_MMAN_H */
+#endif /* HAVE_SYS_MMAN_H && !HB_NO_MMAP */
 
 
 /**
